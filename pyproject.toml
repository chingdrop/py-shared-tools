[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["shared_tools"]

[project]
name = "py-shared-tools"
version = "1.0.0"
description = "Small, dependency-minimal HTTP/storage/config/vendor-connector utilities shared across personal projects (RestAdapter, ObjectStorage, config env-ref resolution, VendorConnector/remote_exec, SentinelOne RSO mixin, script-export storage handoff)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "certifi>=2024.7.4",
    "urllib3>=2.0",
]

[project.optional-dependencies]
storage = ["boto3>=1.34"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "moto[s3]>=5.0",
    "boto3>=1.34",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
